/**
 * Re-exported types from react-hook-form used across the form system.
 * Keeps the main types.ts free of direct react-hook-form imports.
 */
export type {
  ComponentType,
} from "react";

export type {
  ControllerRenderProps,
  FieldValues,
  UseFormReturn,
} from "react-hook-form";
